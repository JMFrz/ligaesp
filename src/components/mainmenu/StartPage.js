import MainMenu from "./MainMenu";

function StartPage() {
    return (
        <div className="start-page">
            <MainMenu />
        </div>
    );
}

export default StartPage; // Asegúrate de exportar por defecto